[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/worker/Dockerfile"

[deploy]
startCommand = "node apps/worker/dist/retention-worker.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
