# Retain Source Audio

Cauli retains each Recording’s Source Audio until the user deletes the Recording, even after producing an MP3, WAV, or transcript. This uses more local storage than keeping only derived outputs, but preserves the authoritative capture, prevents downstream failures from causing data loss, and allows export and transcription to be retried.
